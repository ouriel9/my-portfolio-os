streamlit==1.58.0
streamlit-option-menu
streamlit-extras
streamlit-aggrid
streamlit-lottie
streamlit-local-storage
# Native data stack PINNED to a battle-tested combo. Unpinned, Streamlit Cloud pulled bleeding-edge
# pandas 3.0 whose Arrow serialization SEGFAULTED the Linux container ("Oh no. Error running app" on
# the phone) — a native crash a Python try/except can't catch. pandas 2.2 + pyarrow 18 is the combo
# thousands of Cloud apps run without native crashes; verified the app renders cleanly on it (landing,
# trades table, live fragment). Move all four together. (fix: phone Streamlit-Cloud segfault)
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
yfinance
plotly==6.6.0
fpdf
gspread
google-auth
openpyxl
xlrd
pypdf
